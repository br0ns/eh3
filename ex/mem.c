#include <stdio.h>
#include "src/c/cc.eh3/ncc.h"

int main() {
  struct mem m;

  /* mem_init(&m); */

  /* printf("s = %p, sz = %ld, n = %ld\n", m.s, m.sz, m.n); */

  /* mem_put(&m, "hello", 5); */

  /* printf("s = %p(%s), sz = %ld, n = %ld\n", m.s, m.s, m.sz, m.n); */
  return 0;
}
